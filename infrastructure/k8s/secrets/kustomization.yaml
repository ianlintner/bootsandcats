apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - secret-provider-class-oauth2-server.yaml
  - secret-provider-class-profile-service.yaml
  - secret-provider-class-redis.yaml
  - secret-provider-class-github-review.yaml
  - secret-provider-class-slop-detector.yaml
  - secret-provider-class-security-agency.yaml
  # Chat secrets are defined alongside the chat app manifests.
  # Keeping them there avoids conflicting SecretProviderClass definitions.
  # - secret-provider-class-chat.yaml

# NOTE: Template only; do not apply.
# - secret-provider-class-oauth2-client-template.yaml
# - placeholder-secrets.yaml (managed by SecretProviderClass syncing)
