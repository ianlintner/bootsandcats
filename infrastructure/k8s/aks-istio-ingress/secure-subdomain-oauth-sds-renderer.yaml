###################################################################################################
# DEPRECATED / NO-OP
#
# This CronJob rendered a file-based SDS Secret for Envoy's oauth2 filter.
# The current approach uses oauth2-proxy (which reads secrets directly) and does not require SDS.
#
# This file is intentionally a no-op to prevent accidental re-creation of the legacy renderer.
###################################################################################################
apiVersion: v1
kind: List
items: []
