###################################################################################################
# DEPRECATED / NO-OP
#
# This repo previously used Envoy's oauth2 filter with a file-based SDS secret mounted into the
# AKS Istio ingress gateway at /etc/istio/oauth2.
#
# The current approach is oauth2-proxy + gateway Lua (auth_request), so there is no longer any
# SDS secret mount on the gateway.
#
# This file is intentionally a no-op to prevent accidental re-introduction of the legacy mount.
###################################################################################################
apiVersion: v1
kind: List
items: []
